[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.railway.yarn"

[deploy]
healthcheckPath = "/healthz"
healthcheckTimeout = 300

[deploy.multiRegionConfig.us-west2]
numReplicas = 1

[networking]
staticOutboundIp = true