# Arquivo de exemplo de configurações de ambiente
# Copie este arquivo para um arquivo '.env' e preencha com suas configurações reais

# ===== API Keys =====
# Chave da API OpenAI para modelos GPT
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Chave da API Anthropic para modelos Claude (opcional)
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Chave da API de imagem para o módulo de visão (opcional)
VISION_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ===== Configurações de Banco de Dados =====
# String de conexão para o PostgreSQL
POSTGRES_CONNECTION_STRING=postgresql://postgres:postgres@localhost:5432/meu_nutri
# Detalhes separados (opcional, para mais flexibilidade)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=meu_nutri
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# ===== Configurações de Armazenamento =====
# Diretório para armazenar visualizações de análises corporais
VISUALIZATIONS_DIR=/tmp/meu-nutri/visualizations
# Limite de tamanho para upload de imagens (em MB)
MAX_UPLOAD_SIZE=10
# Tempo de retenção para imagens temporárias (em dias)
IMAGE_RETENTION_DAYS=30

# ===== Configurações Alexa =====
# URL base da API para integração com Alexa
MEUNUTRI_API_ENDPOINT=http://0.0.0.0:8000
# Chave de API para autenticação da Alexa com o backend
MEUNUTRI_API_KEY=chave_teste_local_1234567890
# ID da Skill Alexa (opcional, para verificação adicional)
ALEXA_SKILL_ID=amzn1.ask.skill.xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# ===== Configurações de Serviço =====
# Nível de log (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO
# Porta para o servidor API
API_PORT=8000
# Host para o servidor API (0.0.0.0 permite acesso externo)
API_HOST=0.0.0.0
# Habilitar modo de depuração (True/False)
DEBUG=False
# Ativar HTTPS (True/False, requer certificados)
ENABLE_HTTPS=False
# Limite de requisições por minuto por usuário
RATE_LIMIT=60

# ===== Configurações de Segurança =====
# Chave secreta para tokens JWT (gere uma chave forte e única)
JWT_SECRET_KEY=chave_muito_secreta_para_autenticacao_substitua_por_valor_aleatorio
# Tempo de expiração de tokens em minutos
JWT_EXPIRATION_MINUTES=1440
# Algoritmo para tokens JWT
JWT_ALGORITHM=HS256
# Permitir CORS para estes domínios (separados por vírgula)
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://meunutri.com
# Chave para criptografia de dados sensíveis
ENCRYPTION_KEY=chave_de_criptografia_32_caracteres

# ===== Configurações do Agente =====
# Modelo LLM a ser usado (gpt-4, gpt-3.5-turbo, claude-3-opus, etc.)
LLM_MODEL=gpt-4
# Temperatura para geração (0.0 a 1.0, menor é mais determinístico)
LLM_TEMPERATURE=0.7
# Tamanho máximo de tokens para respostas
LLM_MAX_TOKENS=1000
# Número máximo de ferramentas que podem ser usadas em uma consulta
MAX_TOOLS_PER_QUERY=3
# Tempo máximo para processamento de uma consulta (em segundos)
QUERY_TIMEOUT=30
